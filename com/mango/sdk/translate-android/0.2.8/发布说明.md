SDK 0.2.8 将排队、请求和回退纳入统一截止时间，增加单请求取消、相同在途请求合并和可关闭的短期内存缓存，减少连续翻译时的重复请求与无效等待。

新增连接编号、DNS/建连/TLS/响应等待/读取分段耗时，以及请求链、排队和主线程交付耗时。换网后恢复预热，并仅释放网络故障冷却；服务端限流、鉴权拒绝和证书错误冷却保留。引擎身份、独立配置、会员规则及额度边界保留。

- Maven：`com.mango.sdk:translate-android:0.2.8`
- 公开仓库：https://showlcw.github.io/MangoTranslationSuite-Maven/
- 默认行为：非 AI 完成结果缓存 15 秒；`TranslationSdk.configureResultCache(0)` 可关闭。AI 和回退结果不缓存。
- 验证：根验证通过，SDK 142、平台 25、自建服务 61 项测试通过；vivo V2229A / Android 16 隔离真机测试 4 项通过，Release AAR 构建通过。
- 真机验证覆盖本地测试服务与 SDK 调度；生产首翻延迟收益及宿主 R8 包完整回归仍需接入方复测。

随包提供完整接入指南、首次翻译验收文档、优化与验证说明、AAR、POM、module、sources JAR 和 SHA256SUMS.txt。
