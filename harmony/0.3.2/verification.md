# Harmony SDK 0.3.2 / Android 0.2.17 对齐验证

2026-09-19。鸿蒙版本保持独立递增，功能基线对齐 Android 0.2.17。

两端单次超时：所有自建5秒、普通第三方3秒、AI 10秒。Google专用接口兼容保留，但无法突破5秒上限。短剩余预算不延长、不发送不足2秒的自建请求，不将本地预算不足写入Provider冷却；原生传输携带匿名链路ID。

鸿蒙密钥重试共用引擎截止时间；分批沿用同一预算，超时取消后切换。同步Android最新25引擎、自有语言声明、Youdao范围和DeepL备用顺序。389条基础字符串、141个本地化目录。

## 已验证

- Android 181项单测通过；Release AAR构建通过，Lint 0错误、8警告。
- Android已连接华为EML-AL00和API29模拟器，各4项设备测试通过，共8项。
- 根契约校验、平台37项测试、self-hosted 88项测试通过。
- 鸿蒙81项逻辑/适配器测试通过，包含5秒取消切换、迟到结果隔离、短预算不发送/不冷却、链路ID跨回退。
- DevEco 26原生release HAR编译通过；保持最低兼容API12，构建目标更新到API26。
- HAR包内源码、25个引擎和156个资源文件校验通过。
- 独立consumer从最终HAR安装，源码/资源内容校验与HAP编译通过。

HAR：mango-translate-harmony-0.3.2.har，4,502,448字节。

SHA-256：`da88e902f1eedb5f5f18a9616b68f0c0f84354962aae1c5d345621e8a934c640`

## 发布边界

鸿蒙HAR作为公开预览附件提供，未发布到OHPM注册中心。`hdc list targets`为空，consumer HAP未签名；鸿蒙实机、最低API运行、专属凭据下真实Provider和宿主生命周期验收尚未完成。编译和模拟API测试不代表这些验收通过。

编译仍有平台能力/异常处理等警告，不宣称零警告。没有修改业务App、跨App复用凭据、借用签名或部署服务器。宿主升级依赖并重新发布后，新超时策略才对已安装App生效。
